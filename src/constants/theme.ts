import { MD3LightTheme as DefaultTheme } from "react-native-paper";

const theme = {
	...DefaultTheme,
	colors: {
		...DefaultTheme.colors,
		primary: "#272635",
		secondary: "#D1D0D0",
		background: "#EDE3E4",
		trietary: "#9D6381",
	},
};

export default theme;
